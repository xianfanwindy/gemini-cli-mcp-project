# Gemini CLI 新手教程 (3)：资深老手

恭喜你！你已经准备好探索 Gemini CLI 的极限能力了。

## 1. 调度子智能体 (Sub-agents)
作为“资深架构师”，你应该学会调度专门的子智能体：
- **`codebase_investigator`**：当你遇到一个跨文件的诡异 Bug，或者需要理解复杂的系统依赖时，调用它。
- **`generalist`**：面对海量的、重复性的任务（如给 100 个文件添加 License 头部），交给它处理以节省主对话的上下文空间。

## 2. 内存管理 (MEMORY.md)
除了项目共享的 `GEMINI.md`，你还有私人的 `MEMORY.md`。
- **路径**：通常在 `~/.gemini/tmp/.../memory/` 目录下。
- **用途**：记录你个人的偏好、本地特定的配置或只有你知道的小技巧。它不会被提交到 Git，是你的私人“大脑”。

## 3. GitHub MCP 实战：自动化仓库管理
作为资深用户，你可以通过 **Model Context Protocol (MCP)** 将 Gemini CLI 与 GitHub 深度集成。
- **功能**：让 AI 协助你列出 Issue、创建 Pull Request 或跨仓库搜索代码。
- **配置**：确保 `mcp-config.json` 中配置了正确的 `GITHUB_PERSONAL_ACCESS_TOKEN`。
- **实战指令示例**：
    > "请帮我检查仓库中最近 3 个打开的 Issue，并总结它们的核心诉求。"
    > "根据当前的修改，为我创建一个指向 main 分支的 PR，并写好描述。"

## 4. 复杂任务的策略化
面对大型任务，学会与 Gemini 协作分步执行：
1. **研究阶段**：让 Gemini 映射架构。
2. **原型阶段**：在临时目录或分支尝试实现。
3. **集成阶段**：逐步合并并进行详尽的验证。

## 4. 安全与完整性
- **保护敏感信息**：资深用户永远不会让 Gemini 接触 `.env` 或密钥文件。
- **工程化思维**：始终要求 Gemini 在修改代码的同时更新相关的单元测试和文档。

## 5. 迁移至 Antigravity
作为资深用户，请关注工具的演进。请在 6 月 18 日前完成向 **Antigravity CLI** 的迁移，以体验更强大的多智能体平台。

---
*祝你在 AI 辅助编程的道路上越走越远！*
