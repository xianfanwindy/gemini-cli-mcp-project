# Gemini CLI 项目开发提示词 (适配版)

本提示词专为当前 **Gemini CLI 文档与教程项目** 定制，旨在引导 AI 助手进行自主开发、QA 测试及进度管理。

## 提示词全文

请先判断当前 **Gemini CLI 文档与教程项目** 的状态，使用 `agent-browser` 进行必要测试和 QA（例如验证文档中的外部链接、模拟用户查阅流程），然后自主选择本轮最合适的工作重点（修正过时文档、完善 Antigravity 迁移指南或开发新的教程章节）并继续。

**要求：**
1.  **查阅进展**：开始任务时必须查看 `/home/abc/.gemini/tmp/20260520/memory/worklog.md` 了解实际进度，并拆解本轮详细的 TODO。
2.  **QA 与修复优先**：判断项目状态，使用 `agent-browser` 进行自动化检查（运行 `agent-browser --help` 查看可用命令）。若发现文档链接失效、Antigravity 迁移命令错误、教程步骤无法复现或描述存在歧义，优先修复。
3.  **自主功能演进**：若当前文档稳定且准确，则自主提出新需求。例如：增加“Antigravity 实战案例”教程、深化 `TUTORIAL_3_ADVANCED.md` 中的子智能体调度内容、或为 `GEMINI_GUIDE.md` 增加常见问题排查表。
4.  **细节极致化**：Markdown 排版要精美（多使用表格、警告/提示块、代码高亮），确保每一条 CLI 命令都有明确的预期输出说明。
5.  **内容持续扩展**：功能点覆盖要全，尤其是针对 6 月 18 日前的迁移步骤，必须做到无死角覆盖。
6.  **更新工作日志**：工作告一段落后更新或创建 `/home/abc/.gemini/tmp/20260520/memory/worklog.md`（期间建议及时更新，防止进度丢失）。
7.  **交接记录**：任务结束后在 `worklog.md` 中必须包含：
    *   **项目当前状态判断**（如：迁移指南已完成 80%，教程 1-2 已验证）。
    *   **当前目标/已完成的修改/验证结果**（如：修复了 3 处链接，新增了 agy login 步骤说明）。
    *   **未解决问题或风险**（如：Antigravity 部分高级 API 尚未发布，需下周跟进）。

---

## 使用指南
你可以将上述提示词直接粘贴给 Gemini CLI，或者在开启新一轮开发任务时作为参考指令。
