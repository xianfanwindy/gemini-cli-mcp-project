# Gemini CLI 新手指南

欢迎使用 Gemini CLI！本指南将帮助你快速上手并高效利用这一强大的 AI 编程助手。

## 1. 快速入门
- **提问与对话**：你可以直接输入编程问题、请求代码审查或要求解释复杂逻辑。
- **运行命令**：Gemini 可以帮你执行 shell 命令（会先请求你的确认）。
- **编辑代码**：通过描述你想要的变化，Gemini 可以直接修改你项目中的文件。

## 2. 核心功能
- **自动编辑模式 (Auto-Edit)**：这是你当前所处的模式，Gemini 可以自主研究、制定策略并执行代码修改。
- **子智能体 (Sub-agents)**：
    - `codebase_investigator`：用于深度分析代码库、查找 bug 根因或理解系统架构。
    - `generalist`：处理大批量任务（如全项目重构）。
- **计划模式 (Plan Mode)**：在进行重大架构调整或创建新应用前，Gemini 会先编写设计文档供你审批。

## 3. 关键指令
- `/help`：查看详细的帮助信息和可用命令。
- `/bug`：报告错误或提供反馈。

## 4. 自定义交互 (GEMINI.md)
你可以在项目根目录或子目录中创建 `GEMINI.md` 文件。
- **用途**：定义项目特定的架构准则、命名规范、技术栈偏好或工作流。
- **优先级**：Gemini 会优先遵守这些文件中的指令，确保生成的代码符合你的团队标准。

## 5. 最佳实践
- **明确具体**：描述任务时越具体，效果越好（例如：指定文件名、函数名或预期的输入输出）。
- **利用上下文**：如果你觉得 Gemini 漏掉了某些信息，可以提示它查看特定的文件。
- **验证结果**：Gemini 修改代码后，建议运行测试或 lint 检查以确保正确性。

## 6. 重要通知：迁移至 Antigravity
Gemini CLI 正在向 **Antigravity CLI (agy)** 迁移。

**状态：已安装**
我已经为你成功安装了 Antigravity CLI。其路径为 `/home/abc/.local/bin/agy`。

### 剩余操作（需手动执行）：
1. **更新环境变量**：
   为了在任何地方都能直接使用 `agy` 命令，请在终端运行：
   ```bash
   source ~/.bashrc
   ```
2. **登录账户**：
   运行以下命令进行身份验证：
   ```bash
   agy login
   ```
   *注意：请在 6 月 18 日前完成此操作以避免服务中断。*

- 详情请访问：[https://goo.gle/gemini-cli-migration](https://goo.gle/gemini-cli-migration)

---
*提示：随时可以问我“你能帮我做什么？”来探索更多功能。*
