# Gemini CLI 新手教程 (2)：中级实战

既然你已经掌握了基础操作，现在让我们深入了解如何让 Gemini 更高效地为你工作。

## 1. 使用 GEMINI.md 定制规范
在项目根目录创建一个 `GEMINI.md` 文件。在这里，你可以写下：
- **项目风格**："本工程统一使用 Tab 缩进。"
- **技术选型**："我们首选 FastAPI 而不是 Flask。"
- **工作流**："修改代码后请务必运行 `npm test`。"
Gemini 会自动读取并遵守这些规则。

## 2. 计划模式 (Plan Mode)
当你需要进行复杂的重构或开发新功能时，可以触发 **Plan Mode**。
- **场景**："我想要重构整个认证模块。"
- **流程**：Gemini 会先生成一份详细的设计文档。在你点击“批准”之前，它不会改动任何代码。这保证了复杂任务的受控性。

## 3. 上下文管理
Gemini 拥有强大的代码搜索工具。
- **主动引导**："请搜索项目中所有使用了 `User` 模型的地方，并将它们改为 `Member`。"
- **精准阅读**：Gemini 会利用 `grep_search` 和 `glob` 高效定位，无需你手动复制粘贴代码。

## 4. 验证与反馈
不要只让 Gemini 写代码，还要让它验证代码。
- **验证指令**："修改完后，请帮我运行测试用例并修复可能出现的 bug。"

---
*下一篇预告：[高级教程：资深老手](./TUTORIAL_3_ADVANCED.md)*
